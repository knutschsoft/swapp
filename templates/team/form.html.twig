{% extends 'base.html.twig' %}
{% block title %}{{ 'team.create.title'|trans }}{% endblock %}
{% block content %}
    <div class="card">
        <div class="card-header">
            <h1 class="h5 mb-0">
                {{ 'team.create.headline'|trans }}
            </h1>
        </div>
        <div class="card-body pl-1 pr-1 pb-2">
            {%
                form_theme form
                    'form/form_theme_with_buttons.html.twig'
                    'jquery.collection.html.twig'
            %}

            {{ form_start(form) }}
            {{ form_errors(form) }}

            {{ form_row(form.name) }}
            {{ form_row(form.ageRanges) }}
            {{ form_row(form.users) }}

            <div class="input-group input-group-sm">
                <button class="btn btn-block btn-primary form-control" type="submit">
                    {{ 'team.create.save'|trans }}
                </button>
                <div class="input-group-append">
                    <span class="input-group-text"><i class="ion ion-md-add"></i></span>
                    <span class="input-group-text"><i class="ion ion-md-people"></i></span>
                </div>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

{% block footer_back_href %}
    {{- path('team_list') -}}
{% endblock %}
