{% set personsMale %}
    {% if walk.getMalesCount %}
        {{ walk.getMalesCount }}
    {% else %}
        keine männlichen Personen angetroffen
    {% endif %}
{% endset %}

{{ include('walk/showItem.html.twig', {'key': 'angetroffene männliche Personen', 'value': personsMale}, with_context = false) }}
