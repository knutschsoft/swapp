tag:
 is_not_unique: Ein Tag mit dem Namen "{{ name }}" und der Farbe "{{ color }}" existiert bereits.
way_point:
  contacts_count_not_set: Die Anzahl der Kontakte muss gesetzt sein.
  contacts_count_set: Die Anzahl der Kontakte darf nicht gesetzt sein.
  file:
    max-size: Das Bild ist mit {{ size }} {{ suffix }} größer als die maximal erlaubten {{ limit }} {{ suffix }}.
user:
  not_found: Der Nutzer "{{ value }}" existiert nicht.
  not_found_or_disabled: Der Nutzer "{{ value }}" existiert nicht oder sein Account ist inaktiv.
  password_not_compromised: Dieses Passwort war Teil eines Datenlecks und gewährleistet keine sichere Nutzung. Bitte wähle ein sicheres Passwort.
  email_is_not_unique: Ein Benutzer mit der E-Mail "{{ value }}" existiert bereits.
  username_is_not_unique: Ein Benutzer mit dem Benutzeramen "{{ value }}" existiert bereits.
client:
 name_is_not_unique: Ein Klient mit dem Namen "{{ value }}" existiert bereits.
 email_is_not_unique: Ein Klient mit der E-Mail "{{ value }}" existiert bereits.
walk:
 isStartTimeBeforeEndTime: Die Rundenstartzeit muss vor der Rundenendzeit liegen.
