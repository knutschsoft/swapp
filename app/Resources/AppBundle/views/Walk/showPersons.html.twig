{% set persons %}
    {% if walk.getMalesCount + walk.getFemalesCount %}
        {{ walk.getMalesCount + walk.getFemalesCount }}
    {% else %}
        keine Personen angetroffen
    {% endif %}
{% endset %}

{{ include('@App/Walk/showItem.html.twig', {'key': 'angetroffene Personen', 'value': persons}, with_context = false) }}
