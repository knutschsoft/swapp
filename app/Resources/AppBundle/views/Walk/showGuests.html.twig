{% set guests %}
    {% for guest in walk.getGuests %}
        {{ guest.getName }}
    {% else %}
        Es haben keine Gäste teilgenommen.
    {% endfor %}
{% endset %}

{{ include('@App/Walk/showItem.html.twig', {'key': 'Gäste', 'value': guests}, with_context = false) }}
